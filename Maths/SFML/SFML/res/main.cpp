#include <vector>
#include <cstring>
using namespace std;

class Solution {
public:
    void merge(vector<int>& nums1, int m, vector<int>& nums2, int n) {
       vector<int> result[m+n];
       
// ...
memcpy(result, nums1 , m * sizeof(int));
        
    }
};